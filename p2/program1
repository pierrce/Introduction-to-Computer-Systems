#include <studio.h>
#include <stlib.h>
#include <time.h>

int main(int argc, char** argv) {

  while(1) {
    time_t curtime;
    time(&curtime);
    printf("Current time = %s", ctime(&curtime));
  }
  return (EXIT_SUCCESS);
}
